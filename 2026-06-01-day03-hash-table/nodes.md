对于两数之和题目，需要注意寻找与插入的顺序
如果是先进行插入，那么在解决出现两个相同的数之和满足条件时，会出现问题
例如：target为6，输入为3，此时求差为3，进行寻找找到了第一个元素为3，满足条件。但此时只有一个数3，是不满足的
再比如输入第一个3后又输入第二个三，此时会卡在第一个三处，他会认为第一个三就一定找到了

如果先进行判断后在进行插入就可以避免上述的问题
输入为3时，此时还没有进行插入，map容器找不到，因此正常插入
第二轮输入3时，此时找到了求差的位置，保存其下标和当前元素下标